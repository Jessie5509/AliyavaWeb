@model List<Common.DTO.DtoDetallePedido>

@{
    ViewBag.Title = "DetallePedidos";
}

<h2>Detalle pedido</h2>

<p>
   @Html.ActionLink("Volver", "ListarPedidoUrgente", "Pedido", new { @class = "btn btn-primary" })
   
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               Cantidad a preparar
            </th>
            <th>
                Precio por unidad
            </th>
      
            <th>
               idProducto
            </th>
       
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CantidadPreparar)
                </td>
          
                <td>
                    @Html.DisplayFor(modelItem => item.PrecioU)
                </td>
      
                <td>
                    @Html.DisplayFor(modelItem => item.idProducto)
                </td>
          
            </tr>
        }
    </tbody>
</table>
